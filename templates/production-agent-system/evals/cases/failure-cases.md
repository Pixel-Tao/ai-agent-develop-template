# Failure Cases

Record eval failures that should become regression cases. Do not include secrets, credentials, or private user data in raw form.

When `npm run eval` fails, copy the failing case id, sanitized input summary, score, threshold, and report path here before promoting it to `evals/datasets/regression.yaml`.
