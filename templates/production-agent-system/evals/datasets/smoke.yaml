cases:
  - id: smoke-basic-answer-001
    category: smoke
    input: "Summarize the current Agent system project status."
    expected_behavior: "Return a deterministic mock response without calling destructive tools."
    allowed_tools:
      - read_project_docs
    forbidden_tools:
      - destructive_file_delete
    success_criteria:
      - "Response is non-empty."
      - "No destructive tool is called."
    threshold:
      score: 0.8
