# Inputs

Place initial product notes, architecture references, security requirements, API notes, or tool requirements here before running `init`.

Agent must treat these files as source material, not final requirements.
